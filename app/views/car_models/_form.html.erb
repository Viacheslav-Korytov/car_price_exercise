<%= form_for(@car_model) do |f| %>
  <% if @car_model.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@car_model.errors.count, "error") %> prohibited this car_model from being saved:</h2>

      <ul>
      <% @car_model.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :model_slug %><br>
    <%= f.text_field :model_slug %>
  </div>
  
  <%= f.hidden_field :organization_id %>
  
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
